# Copy to .env and fill in. Consumed by compose.yaml.
FORD_CLIENT_ID=
FORD_CLIENT_SECRET=
ABRP_API_KEY=
ABRP_TOKEN=
# Shared secret for the /wake and /sleep Signal endpoints (any long random
# string, e.g. `openssl rand -base64 32`). Unset = endpoints refuse everything.
SIGNAL_SECRET=
